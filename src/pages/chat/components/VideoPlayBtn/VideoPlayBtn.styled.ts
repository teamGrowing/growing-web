import styled from 'styled-components';
import Icon from 'components/common/Icon/Icon';

// eslint-disable-next-line import/prefer-default-export
export const VideoButton = styled(Icon)`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate3D(-50%, -50%, 0);
`;
